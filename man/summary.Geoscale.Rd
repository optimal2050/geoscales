% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoscale-class.R
\name{summary.Geoscale}
\alias{summary.Geoscale}
\alias{summary.geoscales::Geoscale}
\alias{print.summary_Geoscale}
\title{Summarize a Geoscale}
\usage{
\method{summary}{Geoscale}(object, ...)

\method{summary}{`geoscales::Geoscale`}(object, ...)

\method{print}{summary_Geoscale}(x, ...)
}
\arguments{
\item{object}{A \code{\link{Geoscale}}.}

\item{...}{Ignored.}

\item{x}{A \code{"summary_Geoscale"} object (the print method's argument).}
}
\value{
\code{summary()} returns a list of class \code{"summary_Geoscale"}:
\code{name}, \code{desc}, \code{geoframes} (named member counts), \code{unassigned}
(named NA-atom counts), \code{n_atoms}, \code{weights}, \code{weight_totals},
\code{default_weight}, \code{coverage} (see \code{\link[=geoscale_coverage]{geoscale_coverage()}}),
\code{sampled}, \code{parent_name}, \code{nesting} (adjacent-pair table with
offender counts, see \code{\link[=geoscale_nests]{geoscale_nests()}}), \code{geometry} (attached /
n_features / crs), \code{source}.
}
\description{
Complements \code{\link[=print]{print()}} with the quantitative view: per-weight totals
and coverage, the adjacent-geoframe nesting table, and the geometry
status. Returns a \code{"summary_Geoscale"} object (a list) with its own
print method — the mirror of \code{summary.Calendar()} in timescales.
sf-free: geometry is reported from the object itself, never touched.
}
\examples{
summary(geoscale_example())
summary(filter_geoscale(geoscale_example(), "country", "N"))
}
