% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\name{register_geoscale_rule}
\alias{register_geoscale_rule}
\title{Register how a parameter should be recast}
\usage{
register_geoscale_rule(param, rule, weight = NULL)
}
\arguments{
\item{param}{Name of the value column.}

\item{rule}{One of \code{\link{GEOSCALE_RULES}}.}

\item{weight}{Optional weight column name used by \code{sum} (down) and
\code{weighted_mean} (up). \code{NULL} means the Geoscale's default weight.}
}
\value{
Invisibly, the registered entry.
}
\description{
Records the rule (and optionally the weight) to use for a named value
column, so callers of \code{\link[=recast_geoscale]{recast_geoscale()}} need not repeat it. Downstream
packages can register their own parameter maps at load time.
}
\examples{
register_geoscale_rule("capacity", "sum")
register_geoscale_rule("eff", "weighted_mean", weight = "pop")
get_geoscale_rule("eff")
}
