% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{add_area_geoscale}
\alias{add_area_geoscale}
\title{Compute area weights from attached geometry}
\usage{
add_area_geoscale(x, name = "km2", crs = "ESRI:54034")
}
\arguments{
\item{x}{A \code{\link{Geoscale}} with geometry attached.}

\item{name}{Name of the weight column to add.}

\item{crs}{Equal-area CRS used for the measurement. The default, World
Cylindrical Equal Area, is global; a local equal-area projection is more
accurate for a single region.}
}
\value{
A \code{\link{Geoscale}} with the area column added to \verb{@leaftable} and
registered in \code{meta$weights}.
}
\description{
Adds an area column to \verb{@leaftable}, measured on an equal-area projection.
}
\details{
Areas are only as good as the geometry. Cartographic sources are
\emph{generalised for display}: measured on Natural Earth at 1:110m, Chile comes
out 10.6\% too large and Indonesia 3.2\% too small against the same data at
1:10m. Use the finest geometry available, and treat the result as
indicative rather than authoritative.

Geometry carrying no CRS is measured in its own planar units, with a
warning: reprojection is impossible, but a synthetic or teaching map is
still worth weighting by.
}
\examples{
\dontrun{
gs <- add_area_geoscale(gs, name = "km2")
}
}
