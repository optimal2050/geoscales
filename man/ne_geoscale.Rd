% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/naturalearth.R
\name{ne_geoscale}
\alias{ne_geoscale}
\title{Build a Geoscale from Natural Earth}
\usage{
ne_geoscale(
  scale = 110,
  levels = c("continent", "subregion", "country", "feature"),
  weights = c("pop_est", "gdp_md"),
  geometry = TRUE,
  ...
)
}
\arguments{
\item{scale}{Natural Earth scale: \code{110}, \code{50} or \code{10}. Note that \code{110} is
unsuitable for area weights (see details).}

\item{levels}{Level columns, coarsest first.}

\item{weights}{Weight columns.}

\item{geometry}{Attach geometry.}

\item{...}{Passed to the underlying \code{rnaturalearth} function.}
}
\value{
A \code{\link{Geoscale}}, with \code{scale} and \code{source} recorded in \verb{@meta} so the
object is self-documenting and reproducible.
}
\description{
Convenience wrapper: fetches Natural Earth via \code{\link[=ne_source]{ne_source()}} and builds a
\code{\link{Geoscale}} nested \code{continent -> subregion -> country -> feature},
weighted by population and GDP.
}
\details{
\code{feature} is the atom: the Natural Earth unit. \code{country} sits above it as a
grouping of atoms, so it can only ever be coarser than \code{feature}.

Do not derive area weights from \code{scale = 110}; see \code{\link[=add_area_geoscale]{add_area_geoscale()}}.
}
\examples{
\dontrun{
gs <- ne_geoscale(scale = 110)

# roll population up from countries to sub-regions
lf <- S7::prop(gs, "leaves")
pop <- data.frame(country = lf$country, pop = lf$pop_est)
recast_geoscale(pop[!is.na(pop$country), ], gs,
           from = "country", to = "subregion", rule = "sum")
}
}
