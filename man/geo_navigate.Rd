% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{geo_navigate}
\alias{geo_navigate}
\alias{geo_children}
\alias{geo_parents}
\alias{geo_descendants}
\alias{geo_ancestors}
\title{Navigate a region hierarchy}
\usage{
geo_children(x, level, region, to = NULL)

geo_parents(x, level, region, to = NULL)

geo_descendants(x, level, region, to = NULL)

geo_ancestors(x, level, region, to = NULL)
}
\arguments{
\item{x}{A \code{\link{Geoscale}}.}

\item{level}{Level that \code{region} belongs to.}

\item{region}{Character vector of region codes at \code{level}.}

\item{to}{Target level. For \code{geo_children()}/\code{geo_parents()} this defaults
to the adjacent level; for the transitive versions, \code{NULL} means all
levels below/above.}
}
\value{
\code{geo_children()} and \code{geo_parents()} return a character vector of
codes at a single level. \code{geo_descendants()} and \code{geo_ancestors()} span
several levels and so return a \code{data.frame} with columns \code{level} and
\code{region} — a bare character vector would be ambiguous, since the same
code can occur at more than one level.
}
\description{
\code{geo_children()} and \code{geo_parents()} step one level; \code{geo_descendants()}
and \code{geo_ancestors()} follow the transitive closure.
}
\details{
\code{level} is required in every case — region codes are not unique across
levels, so a bare code is ambiguous.
}
\examples{
gs <- geoscale_example()
geo_children(gs, "country", "N")
geo_parents(gs, "state", "N1", to = "country")
geo_descendants(gs, "country", "N")
geo_ancestors(gs, "atom", "A5")
}
