% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\name{GEOSCALE_RULES}
\alias{GEOSCALE_RULES}
\title{Supported aggregation rules}
\format{
A character vector of length 7.
}
\usage{
GEOSCALE_RULES
}
\description{
Each rule defines behaviour in \strong{both} directions. Direction is taken from
the geoframe ranks, so aggregation and disaggregation are one operation:
}
\details{
\describe{
\item{\code{sum}}{Up: sum. Down: split proportionally to the weight.
For extensive quantities (capacity, demand, area, population).}
\item{\code{weighted_mean}}{Up: weight-weighted mean. Down: copy unchanged.
For intensive quantities (efficiency, price, capacity factor).}
\item{\code{mean}}{Up: unweighted mean. Down: copy unchanged.}
\item{\code{copy}}{Up: the common value, erroring if it is not constant.
Down: copy unchanged. For region-invariant scalars.}
\item{\code{sd}}{Up: standard deviation over the atoms (aggregation only;
going down it degenerates to \code{NA} for single-atom groups).}
\item{\code{share}}{Share within parent: each source region's value divided
by the total over its parent group. Unlike every other rule the
result stays keyed at the \strong{source} geoframe -- the recast target
(or \verb{parent=}) names the parent -- so it cannot be mixed with other
rules in one call (the two share rules mix freely, being one
computation). For building distribution keys and normalised
profiles; requires \code{from} to nest within the parent.}
\item{\code{logshare}}{The same computation as \code{share} -- the values ARE
shares -- but figures draw it on a fixed log10 percent scale
(0.01\%..100\%), where \code{share} gets a fixed linear 0..1 scale. Use it
when sibling counts differ by orders of magnitude and the linear
scale flattens the crowded groups.}
}
}
\examples{
GEOSCALE_RULES
}
