% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recast.R
\name{recast_to_geoatoms}
\alias{recast_to_geoatoms}
\alias{recast_from_geoatoms}
\title{Recast region data down to the atom layer, and back}
\usage{
recast_to_geoatoms(
  x,
  gs,
  from = NULL,
  key = NULL,
  values = NULL,
  rule = NULL,
  weight = NULL,
  attach_weight = TRUE,
  collect = NULL
)

recast_from_geoatoms(
  x,
  gs,
  to,
  key = NULL,
  values = NULL,
  rule = NULL,
  na_action = c("drop", "error", "keep"),
  collect = NULL
)
}
\arguments{
\item{x}{The data: for \code{recast_to_geoatoms()} keyed by region code at
geoframe \code{from}; for \code{recast_from_geoatoms()} keyed by atom \code{region}
IDs.}

\item{gs}{The \code{\link{Geoscale}} the data is keyed in (\code{to_geoatoms}) or
aggregated into (\code{from_geoatoms}).}

\item{from}{\code{to_geoatoms} only: geoframe name the codes belong to.
\code{NULL} (default) is inferred as in \code{\link[=recast_geoscale]{recast_geoscale()}}.}

\item{key}{The key column. \code{to_geoatoms}: defaults to \code{from} when that
column exists, otherwise \code{"region"}. \code{from_geoatoms}: default
\code{"region"}.}

\item{values, rule, weight}{As in \code{\link[=recast_geoscale]{recast_geoscale()}}.}

\item{attach_weight}{\code{to_geoatoms} only: attach the \code{weight} column
(default \code{TRUE}).}

\item{collect}{For lazy inputs: materialise (\code{TRUE}) or return the
query (default).}

\item{to}{\code{from_geoatoms} only: target geoframe name.}

\item{na_action}{\code{from_geoatoms} only: what to do with atoms that have
no code at \code{to} -- \code{"drop"} (default, warning), \code{"error"}, or
\code{"keep"} (an \code{NA} region row).}
}
\value{
\code{recast_to_geoatoms()}: one row per (atom x identifier
combination) with columns \code{region}, identifiers, values (and
\code{weight}). \code{recast_from_geoatoms()}: one row per (region x identifier
combination) with columns \code{to}-named region, identifiers, values.
Both in the input's class; lazy in, lazy out.
}
\description{
The two public halves of the \code{from -> atoms -> to} route:
\code{recast_to_geoatoms()} projects region-keyed data DOWN to the atom layer
(one row per atom), and \code{recast_from_geoatoms()} aggregates atom-keyed
data UP into a geoframe's regions. Their composition is
\code{\link[=recast_geoscale]{recast_geoscale()}}, and because the atom rows are keyed by the atom
\code{region} IDs, \code{recast_from_geoatoms(recast_to_geoatoms(x, gs_a), gs_b, to)} recasts across two different Geoscales that share atom keys.
}
\details{
Going down, extensive columns (rule \code{"sum"}) are split across a region's
atoms proportionally to the chosen weight so totals conserve; intensive
columns are repeated. A \code{weight} column (the atom's weight) is attached
by default so that the return trip's \code{"weighted_mean"} reproduces the
source weighting exactly; pass \code{attach_weight = FALSE} to omit it.

Going up, rules act on the atom rows directly: \code{"sum"} sums, \code{"mean"}
averages, \code{"weighted_mean"} uses the \code{weight} column when present (else
the target object's chosen weight), \code{"copy"} requires constancy, \code{"sd"}
is the standard deviation over the atoms.

Both ends run as dplyr pipelines and accept any supported backend (see
\code{\link[=recast_geoscale]{recast_geoscale()}}'s Backends section); the geoscale side of every
join is a small in-memory frame.
}
\examples{
gs <- geoscale_example()
y <- data.frame(country = c("N", "S"), capacity = c(10, 20))
a <- recast_to_geoatoms(y, gs, from = "country", rule = "sum",
                        weight = "km2")
a
sum(a$capacity)  # 30 -- totals conserve

recast_from_geoatoms(a, gs, to = "state", rule = "sum")
}
