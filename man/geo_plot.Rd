% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{geo_plot}
\alias{geo_plot}
\title{Map data onto a Geoscale}
\usage{
geo_plot(
  x,
  data = NULL,
  level = NULL,
  fill = NULL,
  palette = NULL,
  title = NULL,
  subtitle = NULL,
  fill_label = fill,
  label = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{\link{Geoscale}} with geometry attached.}

\item{data}{Optional \code{data.frame} with a code column named \code{level} and the
column named by \code{fill}. When \code{NULL}, region outlines are drawn.}

\item{level}{Level to draw. Defaults to the atom level.}

\item{fill}{Name of the value column in \code{data} to colour by.}

\item{palette}{Viridis palette option (\code{"A"}..\code{"H"}) for the fill scale.
\code{NULL} (default) leaves ggplot2's own scale in place.}

\item{title, subtitle}{Plot titles. \code{NULL} for none.}

\item{fill_label}{Legend title. Defaults to \code{fill}; pass \code{NULL} to drop it.}

\item{label}{Draw region labels. \code{TRUE} uses the display names declared by
the geoscale's \verb{@meta$labels} column when there is one, otherwise the
region codes.}

\item{...}{Passed to \code{ggplot2::geom_sf()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a choropleth of \code{data} at \code{level}. Requires \code{sf}, \code{ggplot2}, and
geometry attached with \code{\link[=geo_attach_geometry]{geo_attach_geometry()}}.
}
\details{
This is the package's single choropleth renderer: callers that know what
their numbers \emph{mean} (which variable, extensive or intensive, what units)
should prepare a \code{data.frame} and hand it here rather than draw their own
\code{geom_sf()}. \code{energyRt::geo_map()} works exactly that way.
}
\examples{
\dontrun{
geo_plot(gs, capacity_by_state, level = "state", fill = "capacity")

# titled, viridis, with region names drawn on
geo_plot(gs, gen, level = "zone", fill = "value",
         palette = "D", title = "Generation", label = TRUE)
}
}
