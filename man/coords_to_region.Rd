% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{coords_to_region}
\alias{coords_to_region}
\title{Map coordinates to the regions that contain them}
\usage{
coords_to_region(x, gs, geoframe = NULL, coords = c("lon", "lat"), crs = 4326)
}
\arguments{
\item{x}{An \code{sf}/\code{sfc} object of points, or a data.frame with
coordinate columns.}

\item{gs}{A \code{\link{Geoscale}} with geometry attached (see
\code{\link[=attach_geometry_geoscale]{attach_geometry_geoscale()}}).}

\item{geoframe}{Geoframe to resolve to; \code{NULL} (default) uses the
finest geoframe.}

\item{coords}{Names of the coordinate columns when \code{x} is a plain
data.frame (x/longitude first, y/latitude second).}

\item{crs}{Coordinate reference system of those columns (default
WGS84); points are transformed to the geometry's CRS before
matching.}
}
\value{
A character vector of region codes, one per row/point of \code{x}.
}
\description{
The spatial twin of \code{timescales::datetime_to_timeslice()}: raw
observations enter the structure here. Each point is matched to the
region at \code{geoframe} (the atoms by default) whose geometry contains
it; points outside every region return \code{NA}. A point exactly on a
shared border is assigned to the first matching region in the
object's canonical order.
}
\examples{
\dontrun{
obs <- data.frame(lon = c(-21.9, -18.1), lat = c(64.1, 65.7), v = 1:2)
obs$region <- coords_to_region(obs, gs)
}
}
