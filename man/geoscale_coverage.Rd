% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{geoscale_coverage}
\alias{geoscale_coverage}
\title{Sampled coverage of a Geoscale}
\usage{
geoscale_coverage(x, weight = NULL)
}
\arguments{
\item{x}{A \code{\link{Geoscale}}.}

\item{weight}{A single weight name for a scalar answer; \code{NULL}
(default) returns the named vector over all declared weights.}
}
\value{
A named numeric over the declared weights, or a single
unnamed numeric when \code{weight} is given.
}
\description{
The spatial mirror of a partial calendar's \code{year_fraction}: the
fraction of the ROOT parent's weight totals that this object still
carries. \code{\link[=filter_geoscale]{filter_geoscale()}} (and \code{\link[=prune_geoscale]{prune_geoscale()}} when it
drops uncovered atoms) record it in \code{meta$coverage}; an object that
was never sampled reports \code{1} for every weight.
}
\examples{
gs <- geoscale_example()
geoscale_coverage(gs)                            # all 1 -- not a sample
geoscale_coverage(filter_geoscale(gs, "country", "N"))
}
