% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{geoscale_autoplot}
\alias{geoscale_autoplot}
\alias{autoplot.Geoscale}
\title{Plot a Geoscale}
\usage{
geoscale_autoplot(
  x,
  weight = NULL,
  fill = c("level", "region"),
  label = TRUE,
  ...
)

autoplot.Geoscale(x, ...)
}
\arguments{
\item{x}{A \code{\link{Geoscale}}.}

\item{weight}{Weight column determining widths. \code{NULL} uses the default.}

\item{fill}{What to colour by: \code{"level"} or \code{"region"}.}

\item{label}{Draw region codes on the rectangles.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the hierarchy as an icicle: one row per level, coarsest at the top,
each region's width proportional to its share of the weight.
}
\details{
This is the \emph{structure} plot — it shows the Geoscale itself and needs no
geometry. For a map of values over regions, see \code{\link[=geoscale_plot]{geoscale_plot()}}.

Also registered as an \code{autoplot()} method, so \code{ggplot2::autoplot(gs)} works
when ggplot2 is installed.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  geoscale_autoplot(geoscale_example())
}
}
