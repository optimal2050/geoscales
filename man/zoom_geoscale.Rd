% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoom.R
\name{zoom_geoscale}
\alias{zoom_geoscale}
\title{Telescoping zoom: fine detail in a focus area, coarse elsewhere}
\usage{
zoom_geoscale(x, focus, levels = NULL, name = "zoom", label_rest = "\%s_rest")
}
\arguments{
\item{x}{A \code{\link{Geoscale}}.}

\item{focus}{Character vector of \strong{atom} codes to keep at full resolution.}

\item{levels}{Ordered geoframes to build rings from, coarsest first.
Defaults to every geoframe of \code{x} except the atom layer.}

\item{name}{Name of the new geoframe. Default \code{"zoom"}.}

\item{label_rest}{\code{sprintf} format for a ring's code, with \verb{\%s} the parent
code. Default \code{"\%s_rest"}.}
}
\value{
A \code{\link{Geoscale}} with geoframes \code{levels[1]}, \code{name} and the atom layer;
weights and geometry carried through unchanged.
}
\description{
Builds a new geoframe that keeps \code{focus} atoms at full resolution and
collapses everything else into progressively coarser rings drawn from
\code{levels}. The result is a partition of the atoms, carried as a geoframe.
}
\details{
With focus atoms and \code{levels = c("nuts0", "nuts1", "nuts2")} (coarsest
first), each remaining atom joins the \strong{finest} ring that also contains a
focus atom, so the cut telescopes outward:
\itemize{
\item the focus atoms themselves, individually;
\item the rest of each \code{nuts2} holding a focus atom;
\item the rest of each \code{nuts1} holding one of those;
\item the rest of each \code{nuts0} holding one of those;
\item every other atom, grouped at \code{levels[1]}.
}

The intermediate geoframes are \strong{dropped}. A ring like "rest of nuts1" spans
several \code{nuts2}, so the cut cannot nest with the levels it was carved from;
only \code{levels[1]} and the atom layer are kept beside it. That is checked, not
assumed — see \code{\link[=geoscale_nests]{geoscale_nests()}}.
}
\examples{
gs <- geoscale_example()
z <- zoom_geoscale(gs, focus = "A1", levels = c("country", "state"))
geoscale_geoframes(z)
geoscale_regions(z, "zoom")
}
\seealso{
\code{\link[=filter_geoscale]{filter_geoscale()}} to sample, \code{\link[=prune_geoscale]{prune_geoscale()}} to coarsen
uniformly, \code{\link[=geoscale_nests]{geoscale_nests()}} for the guarantee this relies on.
}
