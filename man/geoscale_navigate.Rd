% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{geoscale_navigate}
\alias{geoscale_navigate}
\alias{geoscale_children}
\alias{geoscale_parents}
\alias{geoscale_descendants}
\alias{geoscale_ancestors}
\title{Navigate a region hierarchy}
\usage{
geoscale_children(x, level, region, to = NULL)

geoscale_parents(x, level, region, to = NULL)

geoscale_descendants(x, level, region, to = NULL)

geoscale_ancestors(x, level, region, to = NULL)
}
\arguments{
\item{x}{A \code{\link{Geoscale}}.}

\item{level}{Level that \code{region} belongs to.}

\item{region}{Character vector of region codes at \code{level}.}

\item{to}{Target level. For \code{geoscale_children()}/\code{geoscale_parents()} this defaults
to the adjacent level; for the transitive versions, \code{NULL} means all
levels below/above.}
}
\value{
\code{geoscale_children()} and \code{geoscale_parents()} return a character vector of
codes at a single level. \code{geoscale_descendants()} and \code{geoscale_ancestors()} span
several levels and so return a \code{data.frame} with columns \code{level} and
\code{region} — a bare character vector would be ambiguous, since the same
code can occur at more than one level.
}
\description{
\code{geoscale_children()} and \code{geoscale_parents()} step one level; \code{geoscale_descendants()}
and \code{geoscale_ancestors()} follow the transitive closure.
}
\details{
\code{level} is required in every case — region codes are not unique across
levels, so a bare code is ambiguous.
}
\examples{
gs <- geoscale_example()
geoscale_children(gs, "country", "N")
geoscale_parents(gs, "state", "N1", to = "country")
geoscale_descendants(gs, "country", "N")
geoscale_ancestors(gs, "atom", "A5")
}
